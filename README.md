# Demo media for the EARL export and import (PR #94)

This orphan branch only carries the demo recording referenced from the pull request description. It is not meant to be merged.

- `earl-demo-v2.mp4`: 1600x912, 60 fps, H.264, 98 seconds.
- `earl-demo-v2.gif`: 900px wide, 12 fps preview of the same recording.

Recorded on the playground app with Vercel's `agent-browser` driving a headed Chromium on a virtual display, captured with ffmpeg at 60 fps.
